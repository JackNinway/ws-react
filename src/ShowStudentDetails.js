import React from 'react';
import DataTable from './DataTable';
import TableAction from './TableAction';

const ShowStudentDetails = (props) => {
/*    if (DataTable.showDetails) {
        return(    
        <div className="card">
            <div className="card-header bg-dark">
                <h3 className="text-white">Student Info</h3>
            </div>
            <div className="card-body col-sm-5">
                <h6 className='card-title'>{props.student.country} {props.student.city} </h6>
                <p className="card-text">ID: {props.student.id}</p>
                <p className="card-text">Name: {props.student.firstName} {props.student.lastName}</p>
                <p className="card-text">BirthDate: {props.student.birthdate}</p>
            </div>
            <div className="card-footer">
                <button type="button" className="btn btn-danger" 
                        onClick={() => {DataTable.setStudent({}); 
                                        DataTable.setShowDetails(false)}}>Close</button>
            </div>    
        </div>
        )
}     */ 
};

//export default ShowStudentDetails;